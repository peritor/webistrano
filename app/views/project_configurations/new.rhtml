<h2>New configuration for <%= link_to h(current_project.name), project_path(current_project) %></h2>

<% form_for(:configuration, :url => project_project_configurations_path(current_project)) do |f| %>

  <%= render(:partial => 'form')%>

  <p><br />
    <%= submit_tag "Create" %>
  </p>
  
<% end %>
<%= link_to 'Back', project_path(current_project), :class => 'arrow_link'  %>