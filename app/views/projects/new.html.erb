<h2>New project</h2>

<% form_for(:project, :url => projects_path) do |f| %>

  <%= render(:partial => 'form')%>
  
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', projects_path %>