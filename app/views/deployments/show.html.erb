<div id="status_info">
  <%= render(:partial => 'status')%>
</div>
<br />

<%= link_to 'Back to stage', project_stage_path(@project, @stage), :class => 'arrow_link', :id => 'back_link_at_the_bottom' %>

<% content_for(:breadcrumb) do %>
  <% breadcrumb_box do %>
    <%= link_to "Back to stage", project_stage_path(current_project, current_stage), :style => "float:right", :class => "arrow_link" %>
    <span class="h2_light">Project</span> <%= link_to current_project.name, project_path(current_project) %> 
    <span class="h2_light"> Stage</span> <%= link_to current_stage.name, project_stage_path(current_project, current_stage) %>
  <% end %>
<% end %>

<% content_for(:page_title) do %>
  <% @page_title = "Deployment of stage #{h(current_stage.name)} of project #{h(current_project.name)}" %>
  <h2>Deployment: <%=h @deployment.task %></h2>
<% end %>

<% content_for(:page_scripts) do %>

  <script type="text/javascript">
    
    function check_auto_scroll_log(){
      var scroller = $("auto_scroll_log");
      var bottom_marker = $('back_link_at_the_bottom');
      
      if(scroller && scroller.checked && bottom_marker){
        bottom_marker.scrollTo();
      }
    }
    
  </script>

<% end %>
