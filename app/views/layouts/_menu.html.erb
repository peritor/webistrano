
<%= render(:partial => 'layouts/menu_box', :locals => {
                                             :box_type => 'projects', 
                                             :status => controller_in_use_or(ProjectsController, :open, @sidebar_projects), 
                                             :path => projects_path } )%>

<%= render(:partial => 'layouts/menu_box', :locals => {
                                             :box_type => 'hosts', 
                                             :status => controller_in_use_or(HostsController, :closed, @sidebar_hosts), 
                                             :path => hosts_path } )%>

<%= render(:partial => 'layouts/menu_box', :locals => {
                                             :box_type => 'recipes', 
                                             :status => controller_in_use_or(RecipesController, :closed, @sidebar_recipes), 
                                             :path => recipes_path } )%>

<%= render(:partial => 'layouts/menu_box', :locals => {
                                             :box_type => 'users', 
                                             :status => controller_in_use_or(UsersController, :closed, @sidebar_users), 
                                             :path => users_path } )%>


